import { FullScreenIcon } from "@icons/full-screen.icon";
import { UnFullScreenIcon } from "@icons/un-full-screen.icon";

export const FullScreen: React.FC = () => {
  // ---- return jsx ----
  return (
    <>
      <FullScreenIcon className="cursor-pointer" />
    </>
  );
};
